<%= form_with(model: currency) do |form| %>
  <% if currency.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(currency.errors.count, "error") %> prohibited this currency from being saved:</h2>

      <ul>
        <% currency.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :source_currency, style: "display: block" %>
    <%= form.text_field :source_currency %>
  </div>

  <div>
    <%= form.label :target_currency, style: "display: block" %>
    <%= form.text_field :target_currency %>
  </div>

  <div>
    <%= form.label :exchange_rate, style: "display: block" %>
    <%= form.text_field :exchange_rate %>
  </div>

  <div>
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
